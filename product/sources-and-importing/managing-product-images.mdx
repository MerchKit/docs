---
title: "Managing product images"
description: "Upload product photos, browse AI-generated variations, edit and reframe images, and promote them into product image collections from the Merchkit DAM."
---

# Managing Product Images

Product images are a critical input for AI enrichment in Merchkit. The Images page under Catalog in the sidebar is where you manage your product image library.

## What the Image Library Contains

The Images page displays all images associated with your products. This is your centralized library for product photos, lifestyle shots, and any visual content that feeds into Merchkit's enrichment engine.

## How Images Relate to Products

Each product can have one or more images. When you import products, you can map a column in your CSV to the product's image field. Images are indexed and organized by product SKU, making it easy to match images to the right products.

## Using Images for AI Enrichment

Images are powerful inputs for AI-powered attribute generation. When you set up an attribute in Merchkit, you can tell the AI which images to analyze to generate or enrich that attribute.

### Image Source in Attribute Configuration

When creating or editing an attribute:

1. In the attribute settings, look for **Image Source**
2. Select which product images you want the AI to analyze
3. The AI will use those images to generate or enhance the attribute value

This is how Merchkit generates rich, accurate product descriptions and specifications directly from visual data.

[SCREENSHOT: Image Source option in attribute configuration]

### Image Field Type & AI-Generated Images

Some attributes use the **Image** field type. These attributes can generate brand-new images using Merchkit's image generation model (powered by Nano Banana).

**Use cases for AI-generated images:**

- **Dimension line drawings** — generated from product silhouettes to show scale and sizing
- **Lifestyle mockups** — product images placed in real-world contexts
- **Transparent backgrounds** — clean product shots with standardized backgrounds
- **Variations** — color, angle, or zoom variations derived from source images

When you configure an Image field type attribute, you'll specify which source images the AI should use as reference input.

[SCREENSHOT: Image field type with AI generation options]

### Working with Image Variations


Every Image field cell keeps a full history of the images it has generated. Click a cell to open the fullscreen viewer and you'll see a **filmstrip of variations** across the bottom (newest first) and a details panel on the right with the model, aspect ratio, and prompt used for the focused variation.

From any variation you can:

- **Add to collection** — promote the variation into `product_images` (or any other image collection defined on the product) so it appears alongside your other product photos. The variation stays in the history too.
- **Edit** — open the masked inpaint editor: paint over the region you want to change, describe the fix, and generate a new variation with just that area repainted.
- **Reframe** — open the generative reframe/outpaint editor: pick a target aspect ratio (Square, Portrait, Tall, Landscape, Wide, Story) and let Bria expand the canvas to fill in new margins.
- **Use as reference** — feed the variation back into the attribute's reference images so future generations of this column pick up its style.
- **Save**, **Copy URL**, and **Download** — pin a variation so it's excluded from cleanup, copy its URL, or download the file locally.

Edit and reframe results are appended to the same variations list as new entries, so you can always revert by focusing an earlier variation.

### DAM Gallery

The Images page under Catalog is now a workspace-wide **digital asset manager**. It lists every image entity — uploaded product photos and every AI-generated variation across every cell — in a single grid.

Use the filter chips at the top to narrow the view:

- **Generated** — only AI-generated images (variations, edits, reframes)
- **In collection** — only images that have been promoted into an image collection like `product_images`
- **Saved** — only images you've explicitly starred
- **Model** — filter by the generation model that produced the image

Select an image to see its provenance (source image, model, prompt, aspect ratio) and jump to the product it belongs to.

### Retention of Unpromoted Variations

To keep the DAM manageable, generated variations that are **not** promoted into a collection, saved, or used as a lineage source for another image are automatically deleted after 365 days. Uploaded images, saved images, and any variation that's been added to a collection are kept indefinitely. If you want to keep a one-off generated image, star it (**Save**) or add it to a collection.

## Uploading and Managing Images

### Import Images with Products

The most common way to add images is through CSV import. When importing products:

1. Include an image URL column in your CSV
2. During the import mapping step, map this column to the **Image** field
3. Merchkit will fetch and index these images with your products

### Upload Images Directly

You can also upload images directly to the Images page:

1. Click **+ Upload Images**
2. Select image files from your computer
3. Map or confirm which product each image belongs to
4. Click **Save**

[SCREENSHOT: Direct image upload interface]

### Image Best Practices

- Use high-quality images (at least 500x500 pixels recommended)
- Ensure images are in common formats (JPG, PNG, WebP)
- Use clear, well-lit product photos for best AI analysis
- Include multiple angles per product if available — the AI will use these for more comprehensive enrichment

## Next Steps

Now that your images are organized, you're ready to learn how to handle import errors and validation issues. Head to [Import Validation: Understanding Errors & Warnings](/product/sources-and-importing/import-validation-errors).
